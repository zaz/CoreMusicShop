source 'https://rubygems.org'

ruby '2.0.0'

gem 'rails', '4.1.4'
gem 'pg'  # PostgreSQL
gem 'sass-rails', '~> 4.0.3'
gem 'haml-rails'
gem 'uglifier', '>= 1.3.0'  # minify JavaScript
gem 'coffee-rails', '~> 4.0.0'

gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 2.0'

gem 'actionpack-page_caching'

gem 'sdoc', '~> 0.4.0', group: :doc

group :production do
  gem 'rails_12factor'  # serve static assets, log to stdout
end

group :development do
  gem 'spring'
end
